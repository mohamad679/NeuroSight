-r requirements.txt

# Test and quality tooling. Upper bounds prevent silent major behavior changes
# in CI while still allowing patch updates.
pytest>=8.0,<8.4
pytest-asyncio>=0.23,<0.25
pytest-timeout>=2.3,<2.4
httpx>=0.27,<0.28
ruff>=0.4,<0.6
mypy>=1.10,<1.12
pytest-cov>=5.0,<5.1
hydra-core>=1.3,<1.4
